## Take home tana test

### Tech stack
 - Programming Language: Java 17
 - JDK: Oracle Open JDK 17.0.12

### Questions Summary
#### Question1_GetLongestString
- Given the condition for valid characters and non repetitveness.A function with a for loop iterates in the array of strings for each string being passed to check if passes the conditions and then compare it with default string declared and another function validates or checks if the conditions are adhered to.

#### Question2_firstUniqueProduct
- A function has a for loop which iterates in each string value of product to count occurrences of each string value, and return the first unique string value of product, Otherwise return null if no unique product is found.

#### Question3_twoClosestMinimumDistance
- Find the minimum distance between the two minimum integer values in the array and finding difference between their indexes in the array
- Therefore the difference in between their indexes is the minimum distance

#### Question4_threeMostCommonStringsInSentence
- I used split to divide the sentence into strings , count the number of occurrences of a string and finally arrange or sort them alphabetically

#### Question5_linkedListRotation
- First of all calculate the length of the string, I manually drew the linked list to make it circular, then breaks the circle at the right point to moving each element in the list n times to the right.

### 
